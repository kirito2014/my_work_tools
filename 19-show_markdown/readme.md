将HTML和Markdown文件放在同一目录下，然后使用一个简单的本地服务器运行。例如：

使用Python:  
Python 3:
```bash
python -m http.server 8000
```
Python 2:
```bash
python -m SimpleHTTPServer 8000
```
启动服务器后，在浏览器中访问 http://localhost:8000/markdown_viewer.html。