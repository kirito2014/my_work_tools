测试项
1、单文件场景测试
 - 测试单个文件的读取和处理
 - 测试单个文件的写入和保存
 - 测试单个文件的写入和保存
 - 测试不同检查项都为通过时，是否正确写入到目标文件中
 - 测试不同检查项中，不在检查列表中的项，是否正确标记为/ 
 - 测试不同检查项中，检查项为不通过时，是否正确写入到目标文件中
 - 测试不同检查项中，检查项为全为通过时，是否正确更新 总检查结果为通过结果
 - 测试不同检查项中，检查项为不通过时，是否正确更新 总检查结果为不通过结果
 - 测试不同检查项中，检查项为不通过时，是否正确获取不通过的内容的类型
 - 测试不同检查项中，检查项为未填写的时候，是否正确标记检查个数为对应个数
 - 测试有sheet缺失或名称错误的时候能否正确报错处理
 - 测试有sheet缺失或名称错误的时候，是否正确记录错误信息
 - 测试检查sheet对应关键字段缺失或名称错误的时候，是否正确记录错误信息的详细信息
 - 测试检查sheet对应表不存在的时候，是否正确记录错误信息的详细信息并写入到目标文件中
 - 检查主键非空为多行的时候，是否正确记录错误信息的详细信息并写入到目标文件中
 - 检查主键非空为多行的时候，都为通过时能否正确获取对应的条数信息
 - 检查主键非空为多行的时候，单条或多条不通过时能否正确获取对应的条数信息，并标记检核结果为N并标记测试结果为 不通过

